@model Train
@{
    Layout = "_Layout";
}

<div class="createPlace">
    <form class="createForm form-floating" asp-controller="Train" asp-action="Create" method="post">
            <a asp-controller="Train" asp-action="Index" class="position-absolute top-0 start-100 translate-middle"><i class="bi bi-x-circle fs-2"></i></a>
        
        <div class="row g-2 p-3">
          <div class="col-md">
            <div class="form-floating">             
              <input required asp-for="Name" type="text" class="form-control" id="floatingInputGrid" placeholder="228" value="@Model?.Name">
              @Html.ValidationMessageFor(model => model.Name, null, new { style = "color:red" })
              <label for="floatingInputGrid">Введите наименование поезда</label>
            </div>
          </div>        
        </div>     
        @*@if(ViewBag.Vans != null)
        {            
             <div class="row g-2 p-3">
             <span>Выберите вагоны</span>
             @foreach(Van van in ViewBag.Vans)
            {
                <label style="display: flex; align-items:center;">
                                <input type="checkbox"
                                       name="selectedVans"
                                       value="@van.Id" style="margin-right: 10px;" />
                                @van.Name
                </label>
            }
           
            </div>
        }   *@
    <div class="createButton">
        <button class="btn btn-success" type="submit">
                Создать
            </button>
            <button class="btn btn-primary" type="reset">
                Очистить
        </button>
    </div>
        
    </form>
</div>


