@model IEnumerable<Van>
@{
    Layout = "_Layout";    
}


<div class="container-fluid myContainer">
    <div class="div-contenet-table overflow-auto">
        <table class="table shadow p-3 mb-5 rounded" id="myTable">
            <thead>
                <tr>                    
                    <th></th>
                    <th class="text-center">Индентификатор</th>
                    <th class="text-center">Наименование вагона</th>
                </tr>
            </thead>            
            <tbody id="editClickbody">
                
                @foreach (var el in Model)
                {                             
                    <tr class="rowEditStart">
                        <th class="d-flex justify-content-between">
                            <a asp-action="Edit" asp-controller="Van" asp-route-id="@el.Id"><i class="bi bi-pencil"></i></a>
                            @{
                                if(el.TrainVanSits?.Where(t=>t.Ticket != null).ToList().Count == 0)
                                {
                                    <a asp-action="Delete" asp-controller="Van" asp-route-id="@el.Id"><i class="bi bi-trash"></i></a>                                    
                                }
                                else
                                {
                                    <br>
                                }
                            }
                        </th>                        
                        <th class="editId text-center">   
                            @el.Id
                        </th>
                        <td class="text-center">
                            @el.Name
                        </td>  
                    </tr>
                }              
                
            </tbody>
        </table>

    </div>
</div>

